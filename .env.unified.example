PROJECT_SLUG=kaleem-store
NODE_ENV=production

POSTGRES_USER=kaleem
POSTGRES_PASSWORD=change-me
POSTGRES_DB=kaleem_store

RABBITMQ_DEFAULT_USER=kaleem
RABBITMQ_DEFAULT_PASS=change-me

MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=change-me

# Keep existing API/runtime env variables from your current .env.prod in .env.unified
# Example keys commonly required by API:
# DATABASE_URL=
# REDIS_URL=
# RABBITMQ_URL=
# JWT_SECRET=
